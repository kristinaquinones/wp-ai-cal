# Work summary

## Changes

- Added `CLAUDE.md` as the canonical repository guide.
- Replaced `AGENTS.md` and `.cursorrules` with pointers to `CLAUDE.md`.
- Added pull request and `main` branch CI for PHP syntax, smoke tests, and JavaScript syntax.
- Added advisory WordPress Plugin Check coverage.
- Added tag-triggered plugin ZIP packaging and draft GitHub release creation.
- Added weekly Dependabot updates for pinned GitHub Actions.
- Removed an extra closing brace in the AI client that caused a PHP parse error.

## Rationale

The repository now has one maintained guidance source and automated checks that match its dependency-free WordPress plugin architecture. Release automation creates a reviewable artifact without publishing automatically.

## Validation

- Ran the local PHP smoke test and PHP syntax checks.
- Ran JavaScript syntax checks.
- Validated workflow YAML syntax.
- Built and inspected the release ZIP locally.

## Follow-up

- Review the first Plugin Check run and decide which findings should become required.
